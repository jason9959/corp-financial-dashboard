# DART·SEC API 연결 논의안

## 먼저 정할 것

1. **기업 검색 범위**
   - 한국: DART `corpCode.xml`의 회사명·종목코드·고유번호를 로컬 캐시한다.
   - 미국: SEC `company_tickers_exchange.json`의 회사명·티커·CIK를 캐시한다.
   - 같은 이름이 여러 개면 국가, 거래소, 종목코드를 표시해 사용자가 선택한다.
   - 6자리 한국 종목코드는 DART, 영문 티커는 SEC를 우선하되 최종 판별은 기업 마스터 일치로 한다.

2. **재무제표 범위**
   - 한국은 연결재무제표(CFS)를 우선하고 없으면 별도재무제표(OFS)를 사용한다.
   - 미국은 10-K·10-Q를 기본으로 하며 해외 발행사의 20-F·6-K 포함 여부를 결정한다.
   - 금융회사도 처음부터 포함할지, 일반 사업회사 안정화 후 포함할지 결정한다.

3. **분기 값 계산 원칙**
   - 자산·부채·자본은 보고기간 말 잔액을 그대로 사용한다.
   - 손익·현금흐름은 누적 공시에서 직전 누적값을 차감해 분기 단독값을 계산한다.
   - 1분기는 공시값을 그대로 쓰고, 4분기는 연간값에서 3분기 누적값을 차감한다.
   - 비교 기간, 연결/별도, 통화가 다르면 차감하지 않는다.

4. **계정 매핑**
   - DART는 표준계정 ID를 우선하고 회사별 계정명은 보조로 사용한다.
   - SEC는 `us-gaap` 표준 태그 후보를 지표별 우선순위 목록으로 관리한다.
   - 매출과 현금 항목은 회사마다 태그가 달라 단일 태그에 의존하지 않는다.
   - 사용한 원본 계정명·태그·공시 링크를 결과에 함께 보존한다.

5. **정정공시와 중복 값**
   - 같은 보고기간 값이 여러 번 제출되면 최신 접수본을 기본으로 한다.
   - 원래 공시와 정정 공시를 구분해 조회 시점과 접수번호를 저장한다.
   - 값이 없으면 `0`이 아니라 결측으로 표시한다.

## API 설정

### 한국 DART

- Open DART 계정에서 40자리 인증키를 발급받는다.
- 로컬: `.streamlit/secrets.toml`에 `DART_API_KEY`로 저장한다.
- 배포: Streamlit Community Cloud 앱 설정의 Secrets에 같은 이름으로 저장한다.
- GitHub에는 인증키를 올리지 않는다. 현재 `.gitignore`가 `secrets.toml`을 제외한다.
- 기업 마스터는 고유번호 파일을 주기적으로 갱신하고, 재무정보는 `fnlttSinglAcntAll.json`을 사용한다.

### 미국 SEC

- API 키는 필요 없다.
- 모든 요청에 앱 이름과 연락 가능한 이메일을 포함한 `User-Agent`를 보낸다.
- `SEC_USER_AGENT`를 Secrets에 저장한다. 예: `CorpFinancialDashboard contact@example.com`.
- 기업 검색은 SEC 기업·티커 목록, 재무정보는 `companyfacts/CIK##########.json`을 사용한다.
- SEC의 현재 최대 요청률 10회/초보다 충분히 낮게 제한하고 결과를 캐시한다.

## 권장 구현 순서

1. 기업 마스터 다운로드·검색 및 한국/미국 판별
2. 삼성전자와 Apple의 연도별 데이터 연결
3. 분기 누적값을 단독 분기로 변환
4. 현재 3개 그래프에 실데이터 연결
5. 원문 공시 링크·단위·연결/별도·조회 시각 표시
6. 대표 기업 교차검증 후 CAPEX 추가

## 첫 연결 전에 필요한 사용자 값

- DART API 인증키
- SEC 요청에 사용할 연락 이메일
- 미국 해외 발행사의 20-F·6-K를 첫 버전에 포함할지 여부
- 금융회사를 첫 버전에 포함할지 여부
